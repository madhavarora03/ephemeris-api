[project]
name = "ephemeris-engine"
version = "0.1.0"
description = "IPC-native Swiss Ephemeris computation engine"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pysweph>=2.10.3.6",
]
