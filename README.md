# Ephemeris Engine
> IPC-native Swiss Ephemeris computation engine

## Download ephemeris files locally
```bash
uv run scripts/download_ephe.py
```

This creates the local `ephe/` directory used by the API.
